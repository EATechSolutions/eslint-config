# name: Release
# on: 
#   push:
#     tags:
#       - 'v*'
# jobs:
#   release:
#     name: Create Release
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2
#       - name: Create Release
#         id: create_release
#         uses: actions/create-release@v1
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
#         with:
#           tag_name: ${{ github.ref }}
#           release_name: Release ${{ github.ref }}
#           body: ${{ github.event.pull_request.title }}
#           draft: false
#           prerelease: false

name: Test
on:
  push
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: version
        run: npm version patch
      - name: Step 1
        run: |
          echo "Name=$(git tag  | grep -E '^v[0-9]' | sort -V | tail -1)" >> $GITHUB_ENV
      - name: Step 2
        run: |
          echo ${{ env.Name }}